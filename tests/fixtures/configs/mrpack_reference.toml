# REFERENCE 模式 mrpack 配置
[minecraft]
version = ["1.21.1"]
mod_loader = "fabric"
mods = ["sodium"]

[output]
download_dir = "downloads"
format = ["mrpack"]
mrpack_modes = ["reference"]

[metadata]
name = "TestPack"
version = "1.0.0"
